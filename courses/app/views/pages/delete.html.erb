<div class="hero-unit">
	<h1>Pages#delete</h1>

	<hr>

	<h1>Delete Page</h1>

	<%= form_for(:page, :url => {:action => 'remove', :id => @page.id}, :method => 'post') do |f| %>
		<div style="padding: 15px">
		<p><b>Are you sure you want to delete this position?</b></p>
			<p><%= @page.name %></p>
		</div>
		<div>
			<%= submit_tag('Delete',  :class => 'btn btn-danger') %>
		</div>
	<% end %>

	<br>

	<%= link_to('Back to main page', {:action=> 'index'}, :class => 'btn btn-warning') %>
</div>
