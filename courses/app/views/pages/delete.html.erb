<div class="hero-unit">
  <h1>Pages#delete</h1>

  <hr>

  <h1>Delete Page</h1>

  <%= form_for(:pages, :url => {:action => 'remove', :id => @pages.id}, :method => 'POST') do |f| %>
    <div style="padding: 15px">
    <p><b>Are you sure you want to delete this position?</b></p>
      <p><%= @pages.name %></p>
    </div>
    <div>
      <%= submit_tag('Delete',  :class => 'btn btn-danger') %>
    </div>
  <% end %>

  <br>

  <%= link_to('Back to main page', {:action=> 'index'}, :class => 'btn btn-warning') %>
</div>
